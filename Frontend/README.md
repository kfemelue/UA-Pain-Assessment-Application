#  UA-PAIN-ASSESSMENT-FRONTEND

To run locally:

1. `cd Frontend`
2. Create a .env file based on .env.example or the text below, and fill in the server uri
```commandline
    server_base_uri =
    form_oid=
```
3. `npm run dev`

4. Open a web browser and navigate to http://localhost:5173
